<%= turbo_stream_from @board.lists_channel %>

<ul id="<%= @board.lists_channel %>" data-controller="sortable" class="flex flex-row flex-nowrap max-h-screen overflow-x-auto gap-2" >
  <%= render partial: 'lists/lists', locals: { board: @board } %>
</ul>
