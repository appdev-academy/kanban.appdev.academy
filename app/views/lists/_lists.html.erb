<% board.lists.each do |list| %>
  <li data-list-id="<%= list.id %>" class="flex-none p-2 w-xs rounded-sm bg-zinc-100">
    <%= render list %>
  </li>
<% end %>
<li class="flex-none p-2 w-xs rounded-sm">
  <%= link_to "New List", new_board_list_path(board), data: { turbo_frame: 'modal' }, class: "font-medium text-blue-600 hover:underline" %>
</li>
